This folder contains two example embedding files (.pt):
- `example_embeddings.pt` : a dictionary containing example embeddings of words in Korean and English
- `example_paired_embeddings.pt` : A dictionary containing example translation pairs (Korean words and their English translations, and vice versa), along with the embeddings of each word in the pair

